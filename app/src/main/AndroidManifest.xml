<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto">
        <activity
            android:name=".alertaactivity"
            android:exported="false" />
        <activity
            android:name=".registrateactivity"
            android:exported="false" />
        <activity
            android:name=".loginactivity"
            android:exported="false" /> <!-- Pantalla splash como launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Actividad principal que se muestra luego del splash -->
        <activity android:name=".MainActivity" /> <!-- Otra actividad que tienes -->
        <activity android:name=".homeactivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>